<h1>Form Response:</h1>
<pre>
<code>
<%= @form_params %>
</code>
</pre>
<p>👍</p>

<h2>Lazy technical example notice:</h2>
<p>You will most likely receive <pre>"controller"=>"html", "action"=>"ex02_submit_form"</pre> in the above response. That's fine. We're looking the keys and values in the responses for now.</p>
<%= link_to "Back to Exercise 2", html_ex02_path %>